{
  "context": {
    "example": {
      "max_attempts": 5,
      "target_workflow_lgwf": "D:/path/to/workflow.lgwf"
    },
    "instruction": "Confirm the target workflow.lgwf and self-fix retry limit before any repair work starts.",
    "optional_fields": {
      "max_attempts": "Maximum repair attempts. Defaults to 5."
    },
    "required_fields": {
      "target_workflow_lgwf": "Path to the target workflow.lgwf file."
    }
  },
  "created_at": "2026-06-20T02:08:40.183522+00:00",
  "prompt": "# 确认 Self-Fix 目标\n\n请确认要修复的目标 workflow 和 self-fix 参数。\n\n返回值必须是 JSON object，格式如下：\n\n```json\n{\n  \"target_workflow_lgwf\": \"D:/path/to/workflow.lgwf\",\n  \"max_attempts\": 5\n}\n```\n\n- `target_workflow_lgwf`：必填，目标 `workflow.lgwf` 文件路径。\n- `max_attempts`：可选，最大修复尝试次数，默认 `5`。\n\n确认后，workflow 会读取目标目录，分析目标 workflow 的业务启动参数，并在下一步再次请求确认目标 workflow 的 `--input-json`。\n",
  "request_id": "human-b2e790c8f1b3",
  "status": "pending"
}
