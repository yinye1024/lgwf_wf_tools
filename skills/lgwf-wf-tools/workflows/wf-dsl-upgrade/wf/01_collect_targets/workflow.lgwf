WORKFLOW collect_targets;
ENTRY FLOW main;

DEFAULTS {
  ref_root workflow ".";
  timeout_seconds 900;
}

PY normalize_target_scope
  SCRIPT "scripts/collect_targets.py"
  INPUT state.dsl_upgrade_target
  TIMEOUT 120
  RESULT state.wf_dsl_upgrade.collect_targets_result
  UPDATES_STATE {
    WRITE state.wf_dsl_upgrade.targets;
    WRITE state.wf_dsl_upgrade.collect_targets_result;
  }
  CONTRACT {
    READ state.dsl_upgrade_target;
    WRITE state.wf_dsl_upgrade.targets;
    WRITE state.wf_dsl_upgrade.collect_targets_result;
    WRITE workspace file ".lgwf/target_manifest.json";
    WRITE workspace file ".lgwf/target_scope_validation.json";
  };

FLOW main
  START normalize_target_scope;
