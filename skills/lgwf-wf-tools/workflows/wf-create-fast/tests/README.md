# wf-create-fast tests

本目录只覆盖 `wf-create-fast` 的轻量链路：

- 根 workflow 阶段顺序。
- scaffold materialize 的路径和覆盖规则。
- main-agent handoff payload。

不复制 `wf-create` 的步骤设计、implementation units、post-fix handoff 测试。
