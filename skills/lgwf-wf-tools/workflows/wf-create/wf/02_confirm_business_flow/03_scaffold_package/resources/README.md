# resources

这里存放 `scaffold_package` 子流程的确定性模板和接口说明。

- `scaffold_package_template.json`：机器可读脚手架模板。
- `scaffold_template_spec.md`：面向步骤设计和实现阶段的模板说明。
- `scaffold_result_contract.md`：`scaffold_package_result.json` 的输出契约。
- `scaffold_package_README.md`：目标 package README 占位内容模板。

## 禁止事项

- 不放置业务流 review prompt。
- 不保存运行态 `.lgwf` 文件。
